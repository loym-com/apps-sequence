To get a sequence code in the field configured to store sequence codes:

1. Create a new record. The sequence field should be readonly to the user.
2. Or open a record with empty sequence field,
   click the action menu, and click `Set [name of field]`.
3. The record has a sequence code in the sequence field.
