<odoo>
    <record id="project_project_action_set_unique_code" model="ir.actions.server">
        <field name="name">Set Unique No.</field>
        <field name="model_id" ref="product.model_project_project"/>
        <field name="binding_model_id" ref="product.model_project_project"/>
        <field name="state">code</field>
        <field name="code">
            <![CDATA[
records = records.sorted(key=lambda r: r.id)
records.set_unique_code_and_name()
            ]]>
        </field>
    </record>
    <record id="project_task_action_set_unique_code" model="ir.actions.server">
        <field name="name">Set Unique No.</field>
        <field name="model_id" ref="product.model_project_task"/>
        <field name="binding_model_id" ref="product.model_project_task"/>
        <field name="state">code</field>
        <field name="code">
            <![CDATA[
records = records.sorted(key=lambda r: r.id)
records.set_unique_code_and_name()
            ]]>
        </field>
    </record>
</odoo>
