<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="product_product_view_tree" model="ir.ui.view">
        <field name="name">product.product.view.tree.inherit.unique_code</field>
        <field name="model">product.product</field>
        <field name="inherit_id" ref="product.product_product_tree_view" />
        <field name="arch" type="xml">
            <field name="default_code" position="before">
                <field
                    name="unique_code"
                    optional="hide"
                    string="Product No."
                />
            </field>
        </field>
    </record>
    <record id="product_product_view_search" model="ir.ui.view">
        <field name="name">product.product.view.search.inherit.unique_code</field>
        <field name="model">product.product</field>
        <field name="priority">99</field>
        <field name="inherit_id" ref="product.product_search_form_view" />
        <field name="arch" type="xml">
            <!-- Add search field for unique Product Number -->
            <field name="name" position="after">
                <field
                    name="unique_code"
                    string="Product No."
                    filter_domain="[('unique_code', 'ilike', self)]"
                />
            </field>
             <!-- Add group by filter for Product Template -->
            <filter name="categ_id" position="after">
                <filter
                    name="group_by_product_template"
                    string="Product Template"
                    domain="[]"
                    context="{'group_by':'product_tmpl_id'}"
                />
                </filter>

        </field>
    </record>
    <record id="product_product_view_form" model="ir.ui.view">
        <field name="name">product.product.view.form.inherit.unique_code</field>
        <field name="model">product.product</field>
        <field name="mode">primary</field>
        <field eval="7" name="priority" />
        <field name="inherit_id" ref="product.product_template_form_view" />
        <field name="arch" type="xml">
            <xpath expr="//div[@class='oe_title']" position="after">
                <h2>
                <label for="unique_code" string="Product No." />&#x2063;
                <field name="unique_code" decoration-bf="1" readonly="1" />
                </h2>
            </xpath>
        </field>
    </record>
    <record id="product_product_view_kanban" model="ir.ui.view">
        <field name="name">product.product.view.form.inherit.unique_code</field>
        <field name="model">product.product</field>
        <field name="inherit_id" ref="product.product_kanban_view" />
        <field name="arch" type="xml">
            <field name="name" position="before">
                <field name="unique_code" />
                <br />
        </field>
        </field>
    </record>
</odoo>
